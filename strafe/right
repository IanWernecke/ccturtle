-- catwalk program

local args = {...}

-- Check number of arguments for usage
if #args ~= 1 then
	print( "Argument required for length of run." )
	exit()
end

move.right(tonumber(args[1]))
turtle.turnLeft()

